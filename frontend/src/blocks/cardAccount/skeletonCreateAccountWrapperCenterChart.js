import { el } from 'redom';

export default function skeletonCreateAccountWrapperCenterChart() {
  const wrapper = el('div', {
    className: 'account__wrapper-center-button-wrap skeleton-account skeleton-account-center-chart-wrap',
  });

  return wrapper;
}
