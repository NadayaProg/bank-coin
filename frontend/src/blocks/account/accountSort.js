export default function accountSort() {
  const select = document.querySelector('.choices');
  // console.log(select);

  const selectble = document.querySelector('.choices__list--single');
  console.log(selectble.value);
}

// Функция accountSort работает следующим образом:

// Находит HTML элемент с классом .choices и сохраняет его в переменной select.
// Находит HTML элемент с классом .choices__list--single и сохраняет его в переменной selectble, предполагая что это select.
// Выводит в консоль значение выбранной опции в select элементе.
